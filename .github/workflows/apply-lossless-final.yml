name: Apply final lossless boundaries

on:
  push:
    branches:
      - feature/lossless-multi-qr-share
    paths:
      - .github/workflows/apply-lossless-final.yml
  pull_request:
    branches:
      - dev
    types:
      - synchronize
    paths:
      - .github/workflows/apply-lossless-final.yml
      - scripts/apply-lossless-final.py
      - scripts/lossless-final-src/**

permissions:
  contents: write

jobs:
  patch-test-build-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/lossless-multi-qr-share
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply assertion-guarded final boundaries
        run: |
          python3 scripts/apply-lossless-final.py
          git diff --check
          test ! -e scripts/lossless-final-src
          test ! -e scripts/apply-lossless-final.py
          test ! -e .github/workflows/apply-lossless-final.yml

      - name: Install exact dependencies
        run: npm ci

      - name: Run full unit suite
        run: npm test

      - name: Build Next, TypeScript and Serwist
        run: npm run build

      - name: Commit proven final boundaries
        run: |
          git config user.name "Jordybeer"
          git config user.email "19562111+Jordybeer@users.noreply.github.com"
          git add -A
          git commit -m "Laat grote profielen veilig en zuinig reizen"
          git push origin HEAD:feature/lossless-multi-qr-share
