name: Apply lossless share follow-up

on:
  push:
    branches:
      - feature/lossless-multi-qr-share
    paths:
      - .github/workflows/apply-lossless-share-followup.yml
  pull_request:
    branches:
      - dev
    types:
      - synchronize
    paths:
      - .github/workflows/apply-lossless-share-followup.yml
      - scripts/apply-lossless-share-followup.py

permissions:
  contents: write

jobs:
  patch-test-build-commit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/lossless-multi-qr-share
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply assertion-guarded privacy follow-up
        run: |
          python3 scripts/apply-lossless-share-followup.py
          git diff --check
          test ! -e scripts/apply-lossless-share-followup.py
          test ! -e .github/workflows/apply-lossless-share-followup.yml

      - name: Install exact dependencies
        run: npm ci

      - name: Run full unit suite
        run: npm test

      - name: Build Next, TypeScript and Serwist
        run: npm run build

      - name: Commit proven follow-up
        run: |
          git config user.name "Jordybeer"
          git config user.email "19562111+Jordybeer@users.noreply.github.com"
          git add -A
          git commit -m "Laat verborgen momenten niets opbiechten"
          git push origin HEAD:feature/lossless-multi-qr-share
